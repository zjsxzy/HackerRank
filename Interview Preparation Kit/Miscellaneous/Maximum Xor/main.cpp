#include <bits/stdc++.h>
using namespace std;
#define PB push_back
#define SZ(v) ((int)(v).size())
#define abs(x) ((x) > 0 ? (x) : -(x))
typedef long long LL;

struct Trie {
};

void solve() {
    int n, m;
    cin >> n;
    vector<int> arr(n);
    for (auto &x: arr) {
        cin >> x;
    }
    cin >> m;
    for (int i = 0; i < n; i++) {
        int q;
        cin >> q;
    }
}

int main() {
    solve();
    return 0;
}

